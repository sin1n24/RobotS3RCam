# M5Stack マルチプラットフォーム・ロボット制御ファームウェア 仕様書

本ドキュメントは、M5Stackシリーズ（Atom, StickC, Capsule等）を用いたジョイスティック型コントローラーおよびロボット受光部用ファームウェアの機能、通信プロトコル、およびハードウェア構成について定義する。

---

## 1. プロジェクト概要
本ファームウェアは、単一のコードベースで送信機（コントローラー）と受信機（ロボット）の両方の役割を担い、ESP-NOWまたはBLEを用いて複数のサーボモーターを遠隔操作することを目的とする。

- **バージョン:** v1.0a　軽微な変更はa,b,c,d...と変化し、内部構造は変わるが通信仕様など対外的な変化がない場合は0.1づつ増やす
- **主要ライブラリ:** M5Unified, ESP-NOW, WiFi, BLEDevice, M5Stack-Avatar, Adafruit_NeoPixel

---

## 2. 動作モード仕様
デバイス本体のAボタンをダブルクリックすることで、動作モードを切り替える。設定は内蔵フラッシュメモリ（SPIFFS）の `/param.ini` に保存され、再起動後も保持される。

| モード名 | 内部定数 | 役割 | 通信方式 |
| :--- | :--- | :--- | :--- |
| **L Controler** | `MODE_LCONTROL` | 左利き用送信機。ジョイスティックを左右反転。 | ESP-NOW |
| **R Controler** | `MODE_RCONTROL` | 右利き用送信機。標準設定。 | ESP-NOW |
| **ESP-NOW** | `MODE_ESPNOW` | ロボット（受信機）。サーボを駆動。 | ESP-NOW |
| **UGOKU Pad** | `MODE_UGOKU` | スマートフォン等の外部アプリからの操作を受信。サーボを駆動。 | BLE (GATT) |

---

## 3. ハードウェア仕様

### 3.1 対応ボードとサーボピンアサイン
ターゲットボードごとにサーボモーターのPWM出力ピンが自動的に割り当てられる（PWM周波数: 50Hz）。

| ボード | 最大数 | ピンアサイン |
| :--- | :--- | :--- |
| **M5Capsule** | 5 | G9, G7, G5, G3, G1 |
| **M5AtomS3系** | 4 | G5, G6, G7, G8 |
| **M5Atom** | 4 | G22, G19, G23, G33 |
| **M5StickS3系** | 3 | G0, G1, G8 |
| **M5StickCPlus系** | 3 | G0, G25, G26 |
| **M5StickC** | 2 | G0, G26 |

### 3.2 コントローラー入力
- **アナログ入力:** 0～4095 (12bit ADC) を `-1.0 〜 1.0` にスケーリング。
- **フィルタリング:** 直近10サンプルの移動平均（Moving Average）。
- **不感帯 (Deadband):** 入力値の 10% (`0.1`) を遊びとして設定。
- **ミキシング:** 前進/後退（V軸）と旋回（H軸）から、左右のモーター速度（タンクデフ駆動）を演算。

---

## 4. 通信プロトコル仕様

### 4.1 ESP-NOW (コントローラー ⇔ ロボット)

#### A. ペアリング (ENQ/ACK)
1. **探索:** コントローラーのAボタン長押しにより、全端末宛に `['E', 'N', 'Q']` をブロードキャスト送信。
2. **応答:** ロボット側が受信すると、自身のMACアドレスを含めて `['A', 'C', 'K']` を返信。
3. **保存:** コントローラーがACKを受信すると、相手のMACアドレスを `/mac.txt` に保存し、専用ペアリングを確立。

#### B. 制御パケット構造 (7バイト)
| Byte | 項目 | 内容 |
| :--- | :--- | :--- |
| 0 | 固有信号 | s |
| 1 | 固有信号 | i |
| 2 | 固有信号 | n |
| 3 | 固有信号 | 1 |
| 4 | 左モーター | 実値 (-90～90) + 90 |
| 5 | 右モーター | 実値 (-90～90) + 90 |
| 6 | ボタン状態 | ビットフラグ (Bit0:BtnA, Bit1:OK, Bit2:NG, Bit3:StickSW, Bit4:Trigger) |
受信側は固有コードを確認し、一致していなければ捨てること
ESP-NOWのdefaultのCHは使用しないこと

### 4.2 BLE (UGOKU Pad モード)
- **Service UUID:** `4fafc201-1fb5-459e-8fcc-c5c9c331914b`
- **Characteristic UUID:** `beb5483e-36e1-4688-b7f5-ea07361b26a8`
- **ペイロード:** 19バイト固定長（9ペアの[Channel, Value] + 1バイトのXORチェックサム）。
- 詳細は[公式リポジトリ](https://github.com/UGOKU-Lab/UGOKU-Pad_Arduino)参照

---

## 5. UIおよびフィードバック仕様

### 5.1 ディスプレイ・表情
- **M5Stack Avatar:** 受信したボタン入力（OK/NG/Trigger）に応じて表情（Happy/Angry/Sad/Neutral）を切り替え。
- **ステータス表示:** モード起動時に「E (ESP-NOW)」「U (UGOKU)」等の文字をスクロール表示。スクロール表示については旧プログラム参照。

### 5.2 フルカラーLED制御 (追加機能)
- **ピン:** G26 (M5StickCPlus系で使用)
- **虹色アニメーション:** 常時バックグラウンドで更新（10ms周期）。
- **手元操作:** コントローラーのトリガーボタン（Byte2 Bit4）により、ロボット側のLEDのON/OFFをトグル切り替え。

---

## 6. ライセンス
© 2024 sin1 Released under the MIT license.
